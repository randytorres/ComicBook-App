const styles = {
  headerContainer: {
    alignItems: 'center',
    justifyContent: 'center',
    height: 80,
    paddingHorizontal: 10,
    paddingVertical: 24,
    backgroundColor: '#000',
    borderTopWidth: 20,
    borderTopColor: '#424445'
  },
  headerText: {
    fontSize: 38,
    color: '#fff',
    fontWeight: '700'
  }
}
